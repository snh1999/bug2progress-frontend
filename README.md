# Frontend

This project was generated with [Angular CLI](https://github.com/angular/angular-cli) version 16.0.2.

## Development server

Run `ng serve` for a dev server. Navigate to `http://localhost:4200/`. The application will automatically reload if you change any of the source files.

## Code scaffolding

Run `ng generate component component-name` to generate a new component. You can also use `ng generate directive|pipe|service|class|guard|interface|enum|module`.

## Build

Run `ng build` to build the project. The build artifacts will be stored in the `dist/` directory.

## Running unit tests

Run `ng test` to execute the unit tests via [Karma](https://karma-runner.github.io).

## Running end-to-end tests

Run `ng e2e` to execute the end-to-end tests via a platform of your choice. To use this command, you need to first add a package that implements end-to-end testing capabilities.

## Further help

To get more help on the Angular CLI use `ng help` or go check out the [Angular CLI Overview and Command Reference](https://angular.io/cli) page.

i want you to act as a frontend web developer for this conversation. if i provide u with documentation from api will you be able to make a simple looking frontend? i will style it later (or u can suggest suggestion as well)

should i do one by one or just provide the whole featureset at once

1. it is meant to be a bugtracker system. with necessary features.
2. I feel it would be better if we go about features one by one, you tend to mess big requirements bad way.But for completedness of the answer that will be more or less all the functionalities from the API.here is a list of short functionalities,

   - register
   - login
   - forgot Password
   - reset password
   - logout
   - change password
   - view profile
   - edit profile
   - delete account
   - deactivate account
   - create organization
   - view organization
   - view POST
   - create POST/update post
   - view project
   - create/update project
   - add/update features(and connect to project)
   - create/update ticket
   - create/update comment
   - some sort of dashboard for admin, user, owner (separately)

3. no, i would like to figure that out as i go along
4. no, the documentation i provided is the only api we will use
5. i think i will go with angular (as we have more ways to mess up in react)
6. no, i just finished building the api
